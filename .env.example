# 云开发环境 ID（微信云开发控制台 → 概览/环境 → 环境 ID，形如 xxx-1a2b3c）
# 配置后应用启用云端同步（匿名登录需在云开发控制台「登录授权」开启匿名登录）；
# 留空或删除本文件则保持纯本地模式，行为与改造前完全一致。
#
# 注意：envId 会被打进前端 bundle 公开，**它不是访问控制的依据**。
# 数据隔离请使用应用内的「空间密钥」（右上角云图标 → 空间密钥），
# 或用下面的构建期提示值（该值同样会进入 bundle，仅适合本地/演示）。
VITE_CLOUDBASE_ENV=

# 可选：构建期空间密钥提示值。留空即回到「全局共享」模式（任何拿到 envId 的人都能读写）。
# 生产环境建议留空，改为在每台设备上通过界面手动设置空间密钥。
VITE_CLOUDBASE_SPACE=
